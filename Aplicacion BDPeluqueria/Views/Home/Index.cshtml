@model Aplicacion_BDPeluqueria.Models.CuentaUsuarios.mUsuarios

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Iniciar sesión";
}

<h3>@ViewBag.Title <br /><small>Indique sus datos de inicio de sesión.</small></h3>
<hr />

<div class="row" style="padding: 20px;">
    <section id="loginForm">
        @using (Html.BeginForm("Acceso", "Usuarios", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
        {
            @Html.AntiForgeryToken()

            <div class="form-group">
                @Html.LabelFor(m => m.usuario, new { @class = "col-md-2 control-label" })
                <div class="col-md-4">
                    @Html.TextBoxFor(m => m.usuario, new { @class = "form-control input-sm" })
                    @Html.ValidationMessageFor(m => m.usuario)
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.contraseña, new { @class = "col-md-2 control-label" })
                <div class="col-md-4">
                    @Html.PasswordFor(m => m.contraseña, new { @class = "form-control input-sm" })
                    @Html.ValidationMessageFor(m => m.contraseña)
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(m => m.email, new { @class = "col-md-2 control-label" })
                <div class="col-md-4">
                    @Html.PasswordFor(m => m.email, new { @class = "form-control input-sm" })
                    @Html.ValidationMessageFor(m => m.email)
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-offset-2 col-md-8">
                    <input type="submit" name="login" value="Iniciar sesión" class="btn btn-default" />
                </div>
            </div>

            @Html.ValidationSummary(true, "", new { @class = "alert alert-danger alert-dismissable", style = "margin: 20px 20px 0px 20px;" })

        }
    </section>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}